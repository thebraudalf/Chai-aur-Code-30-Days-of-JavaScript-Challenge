// Performing if-else statements
 
age = 8;

// Using if and else keywords
if(age>=18) console.log(`Your age is ${age}, hence you are eligible to vote.`);

else console.log(`Your age is ${age}, hence you are not eligible to vote.`);
