// Performing operation with template literals

// Using "``" back ticks to create a template literal to create a string
let personName = "User";
let personAge = 18;

console.log(`The name of person is ${personName} and the age of this person is ${personAge}.`);

// Using "``" back ticks to create a multi-line string
let multiLineStr = `Multi-line string in programming is a string that spans
across multiple lines. 
In many programming languages, you can create multi-line strings using 
specific syntax.`

console.log(multiLineStr);
